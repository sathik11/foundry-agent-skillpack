# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.*

# Node (if tooling is added later)
node_modules/

# Python (if tooling is added later)
__pycache__/
*.pyc
.venv/

# APM-generated client deploy folders (regenerated by `apm install`)
.agents/
.claude/
.cursor/
.windsurf/
.codex/
.gemini/
.opencode/
# Note: .github/ is NOT ignored — it holds your hand-authored workflows.
#       APM only writes under .github/{prompts,instructions,chatmodes}/ for copilot.


# APM dependencies
apm_modules/

# Internal artifacts (kept locally, not published)
# Slide drafts + exec narrative belong with the deck author, not in the package repo.
presentations/
EXEC_NARRATIVE_DRAFT.md

